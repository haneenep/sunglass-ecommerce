const Razorpay = require('razorpay');
const crypto = require('crypto');

module.exports = {

    createRazorpayOrder : async (req,res) => {

        try{
            
        }catch(error){
            console.error("Error while creating razorpay order");

        }
    }
}